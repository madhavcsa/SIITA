
% Add folders to path.

addpath(pwd);

cd Auxiliary_files;
addpath(genpath(pwd));
cd ..;

cd libs;
addpath(genpath(pwd));
cd ..;

cd datasets;
addpath(genpath(pwd));
cd ..;

cd proposed;
addpath(genpath(pwd));
cd ..;

